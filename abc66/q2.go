package main

import (
	"bufio"
	"fmt"
	"os"
	"strings"
)

var sc = bufio.NewScanner(os.Stdin)

func nextLine() string {
    sc.Scan()
    return sc.Text()
}


func solve1(string a, string line) int {

}

func solve2(string a, string line) bool {
	line[a.len]
}

func main() {
	sc.Split(bufio.ScanWords)
	s := nextLine()	
	cols := strings.Split(s, "")
	fmt.Println(cols[0:1])

}
